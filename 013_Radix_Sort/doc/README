Алгоритм поразрядной сортировки (radix sort).

Он не ялвяется алгоритомом "на месте" (in place), то есть требуется дополнительная память зависищая от размера N входного массива.

Алгоритм порязрядной сортировки является устойчивым, то есть сохряняет относительный порядок одинаковых ключей.

Время работы алгоритма O(w/d*(N + 2^d)), где числа массива являюется w битовыми числами, d произвольный параметр не превышающего w и означающий по сколько битов сортировать массив. Оптимальный выбор этого параметра: если w < lg(N), то d = w, иначе d = lg(N) (В тестовых задачах от N = 1e7 до N = 1e8 в каестве d выбрано 8).
