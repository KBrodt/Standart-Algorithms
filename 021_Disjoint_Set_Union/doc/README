Реализована струтура непересекающихся множеств (DSU) с основными операциями makeSet, find и unite.

Используются эвристики сжатия пути и объединение по рангу. В данной реализации используется вместо объединения по рангу случайное объединение множеств. Временная сложность всех операций T(N) = O(a(N)), где a(N) обратная функция Аккермана (для N=10^600 a(N) = 4).